#include <iostream>
#include <set>
using namespace std;
int main() {
    int n, m, val; cin >> n >> m;
    set<int> s;
    for(int i = 0; i < n + m; i++) {
        cin >> val;
        s.insert(val);
    }
    for(int x : s) cout << x << " ";
    return 0;
}