#include <iostream>
#include <set>
using namespace std;
int main() {
    int n, val; cin >> n;
    set<int> s;
    for(int i = 0; i < n; i++) {
        cin >> val;
        s.insert(val);
    }
    for(int x : s) cout << x << " ";
    return 0;
}